{% extends 'base.html.twig' %}

{% block title %}Social-Privatsphäre{% endblock %}

{% block body %}
<main>
    <h1>Social-Privatsphäre</h1>
    {{ form_start(form) }}
        {{ form_row(form.messagePolicy) }}
        {{ form_row(form.relationshipPolicy) }}
        <button type="submit">Speichern</button>
    {{ form_end(form) }}
    <p><a href="{{ path('app_social_index') }}">Zurück zu Social</a></p>
</main>
{% endblock %}
