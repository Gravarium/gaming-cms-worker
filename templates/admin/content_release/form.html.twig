{% extends 'base.html.twig' %}{% block title %}{{ heading }} | Gaming CMS{% endblock %}{% block body %}<header class="topbar"><a class="brand" href="{{ path('app_admin_content_release_index') }}">Releases</a></header><main class="editor-shell"><p class="eyebrow">Mehrere Inhalte gemeinsam planen</p><h1>{{ heading }}</h1>{{ form_start(form,{attr:{class:'content-form'}}) }}{{ form_widget(form) }}<button>Speichern</button>{{ form_end(form) }}</main>{% endblock %}
