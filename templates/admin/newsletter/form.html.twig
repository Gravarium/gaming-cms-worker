{% extends 'base.html.twig' %}
{% block title %}{{ heading }} | Gaming CMS{% endblock %}
{% block body %}
<header class="topbar"><a class="brand" href="{{ path('app_admin_notification_newsletter_admin_index') }}">Newsletter</a></header>
<main class="editor-shell"><h1>{{ heading }}</h1>{{ form_start(form,{attr:{class:'content-form'}}) }}{{ form_widget(form) }}<button>Speichern</button>{{ form_end(form) }}</main>
{% endblock %}
